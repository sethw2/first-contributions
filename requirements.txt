# Runtime deps for the venv + pip path. Mirrors [project.dependencies] in
# pyproject.toml. Prefer `uv sync`; this file exists for the plain-venv path.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt -r requirements-dev.txt
#   pip install -e .

numpy>=1.26
pandas>=2.1
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27
# alpaca-py>=0.20   # uncomment when the execution module lands
